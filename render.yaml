services:
  - type: web
    name: karaoke-key-api
    runtime: docker
    rootDir: backend
    dockerfilePath: ./Dockerfile
    branch: dev
    plan: free
    envVars:
      - key: ALLOWED_ORIGINS
        value: "https://frontend-79aa7ysu3-niwa-yuzukis-projects.vercel.app,http://localhost:3000"
